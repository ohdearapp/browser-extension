{
  "private": true,
  "scripts": {
    "format": "prettier --single-quote --write 'src/popup/**/*.{css,js,vue}'",
    "lint": "eslint resources/js --fix --ext js,vue"
  },
  "devDependencies": {
    "prettier": "^1.16.4"
  }
}
